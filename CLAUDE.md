# Монос ХАБЭА систем — ажлын дүрэм

Хөдөлмөрийн аюулгүй байдал, эрүүл ахуйн (ХАБЭА) удирдлагын вэб систем.
Амьд хаяг: **https://monos-hab.vercel.app/kpi/** · 267 ажилтан, 11 алба ашигладаг.

Хэрэглэгч монголоор ярина — **хариултаа монголоор бич**. Техникийн бус хүн тул
кодын нэр томьёогоор бус, юу болсныг энгийн хэлээр тайлбарла.

---

## ⚠⚠ Заавал баримтлах 4 дүрэм

**1. Repo нь НИЙТЭД НЭЭЛТТЭЙ (public).**
API түлхүүр, токен, нууц үг, service account файлыг **хэзээ ч commit хийхгүй**.
Тэдгээр нь зөвхөн Vercel / Cloudflare-ийн шифрлэгдсэн орчны хувьсагчид байна.
Ажилтны нууц үг Firestore `users` дотор задгай байдаг — **чатад хэвлэхгүй**.

**2. `kpi/script.js` заасны дараа хувилбарын дугаарыг `bump.sh`-ээр нэмнэ.**
```
sh bump.sh 272
```
Энэ нь `kpi/index.html` (`script.js?v=272`) БА `kpi/version.json` ХОЁУЛААГ зэрэг
шинэчилнэ. ⚠ **Гараар зөвхөн `index.html`-ийг нэмж БОЛОХГҮЙ** — `version.json`
хоцорвол `pwa-init.js` доторх хувилбар шалгагч зөрүү олж, хэрэглэгч бүрийг
мөнхийн дахин ачаалалд оруулна. Мартвал хэрэглэгчид хуучин хувилбарыг харсаар
байна. Локалд шаардлагагүй.

**3. Мөрийн төгсгөл LF байх ёстой.**
Шинэ компьютерт clone хийсний дараа заавал:
```
git config core.autocrlf false
git config core.eol lf
```
Үгүй бол 1.1МБ `script.js` бүхэлдээ өөрчлөгдсөн мэт харагдаж, хоёр компьютер
байнга зөрчилдөнө. Python-оор бичихдээ `newline=''` заавал өг.

**4. Дата нь ЖИНХЭНЭ — туршилтын орчин байхгүй.**
Локал сервер ч гэсэн ижил R2/Firebase рүү холбогдоно. Хадгалсан зүйл 267
ажилтанд тэр дороо харагдана. Туршихдаа бодит бүртгэл бүү үүсгэ.

---

## Хаана юу байна

```
kpi/script.js        ← гол файл (~1.1 МБ, vanilla JS, нэг IIFE)
kpi/index.html       ← кэш дугаар эндээс
kpi/style.css
sw.js                ← service worker (PWA)
api/push-daily.js    ← Vercel cron, өдөр бүр 02:00 UTC (=УБ 10:00)
api/push-now.js      ← шууд push мэдэгдэл
api/file-token.js    ← R2 бичих HMAC зөвшөөрөл
dev-server.js        ← локал хөгжүүлэлт (доор үз)
```

**Хадгалалт:** Firestore (хэрэглэгч, KPI төлөв) + Cloudflare R2
(`monos-upload.buynt666.workers.dev` — эрсдэл, гарын үсэг, хүсэлт, мэдэгдэл).
⚠ Хуучин түлхүүр `monos2026` **хаагдсан (401)** — бичихийн тулд
`/api/file-token/`-оос HMAC зөвшөөрөл авна.

---

## Хэрхэн нийтлэх

```
git pull --rebase origin main
# … засвар …
git add -A && git commit -m "юу өөрчилсөн" && git push origin main
```
→ GitHub Actions → Vercel → 1–2 минутын дараа амьд.
Явц: `github.com/monoshuns07-glitch/monos-hab-lms/actions`

Буруу зүйл гарвал: `git revert HEAD && git push origin main`

Хэрэглэгч git мэдэхгүй — түүнд зориулсан товчлуур:
`1-ЭХЛЭХ.bat` (татах) · `2-ИЛГЭЭХ.bat` (илгээх) · `ХӨГЖҮҮЛЭХ.bat` (локал)

---

## Локал хөгжүүлэлт

```
node dev-server.js          (эсвэл ХӨГЖҮҮЛЭХ.bat)
→ http://localhost:8080/kpi/
```
Файл хадгалмагц браузер өөрөө шинэчлэгдэнэ. Кэш байхгүй тул `?v=` нэмэх
шаардлагагүй. Доод талд улаан «ЛОКАЛ» туг гарна.

⚠ R2 worker нь CORS-оор зөвхөн `monos-hab.vercel.app`-ыг зөвшөөрдөг тул
`script.js` доторх `TASK_R2` нь localhost үед `/r2` прокси руу чиглэдэг
(`dev-server.js` сервер талаас дамжуулна). Vercel дээр огт нөлөөгүй.

---

## Шалгах арга (⭐ маш чухал)

**Офлайн harness** — `script.js`-ийн IIFE бүрхүүлийг тайлж, DOM stub нэмээд
Node дээр аппын БҮХ жинхэнэ логикийг ажиллуулна. 267 ажилтан бүрд яг юу
харагдахыг хөтөчгүйгээр шалгана. Функцийг regex-ээр таамаглахаас хавьгүй
найдвартай. (Скриптүүд нь сешний scratchpad-д — `mkharness.py`, `allmx.js`.)

**Хөтчийн тест** — Chrome DevTools Protocol + Firebase custom token-оор
жинхэнэ ажилтнаар нэвтэрч дэлгэцийг уншина.
⚠ `script.js` нь IIFE тул `Runtime.evaluate`-ээр дотоод функц дуудаж
**болохгүй** — DOM-оор шалга.

**Гүйцэтгэл** — датаг 2×/4×/8× өсгөж хэмжинэ. Өсөлт шугаман байх ёстой.
Үржвэрээр өсвөл хаа нэгтээ давхар давталт бий.

---

## Салбарын үндсэн ойлголт

**Эрсдэлийн оноо:** `R = P × N × H`
(P магадлал, N үр дагавар, H ажилчдын санал — тус бүр 1–5)

| Түвшин | R | Нэр |
|---|---|---|
| A | >100 | Үл зөвшөөрөгдөх |
| B | 51–100 | Ихээхэн |
| C | 11–50 | Дундаж |
| D | 4–10 | Зөвшөөрөгдөх |
| E | ≤3 | Бага |

⚠ Түвшний нэрийг **зөвхөн `RISK_LEVELS`-ээс** ав. Өмнө нь гурван өөр
нэршил зэрэг оршиж, нэг зүйлийг «Ихээхэн / Их / Өндөр» гэж дууддаг байв.

**Гарын үсгийн шатлал (доороос дээш):**
ажилтан → менежер → албаны хариуцагч → Үйлдвэрлэлийн захирал → Гүйцэтгэх захирал.
Доод шат зураагүй бол дээд шат нээгдэхгүй.

**Эрх:** `admin` (ХАБЭА мэргэжилтэн, бүх алба) · `depthead` туслах админ
(зөвхөн өөрийн алба) · `employee`.
⚠ `depthead` эрх нь Firestore `kpi_state/main.userRoles`-д хадгалагддаг
бөгөөд DB ачаалагдсаны **дараа** л мэдэгддэг (`applyRoleOverride`).

---

## Түгээмэл алдаанууд

- **Кирилл дээр `\b` regex ажиллахгүй.** «шил» гэж хайвал «шилжүүлэх»-д таарна.
- **Нэг зүйлийг хоёр газар тоолохгүй.** Дэлгэц дээрх бүх тоо нэг олонлогоос
  гарах ёстой (давхардал нэгтгэсэн). Өмнө нь таб «515», хуудас «361» гэж
  зөрдөг байв.
- **Python-оор файл бичихдээ** түр файлд бичээд, хэмжээг шалгаад,
  `shutil.move` хий. `open(p,'w')` нь бичихээс өмнө файлыг тэглэдэг —
  ингэж `script.js` нэг удаа устсан.
- **⚠ Vercel-ийн үнэгүй багц ЗӨВХӨН 12 serverless функц зөвшөөрнө.** `api/` дотор 13 дахь файл нэмэхэд deploy ЧИМЭЭГҮЙ УНАНА (амьд сайт хуучин хувилбараар үлдэнэ — эвдрэхгүй ч шинэ код гарахгүй). Нэмэхийн өмнө: `ls api/*.js | grep -v '/_' | wc -l`. `_` угтвартай файл функц болдоггүй — хуваалцсан кодыг тэнд тавь (`_smtp.js`, `_otpstore.js`). Хязгаарт хүрвэл нэг сэдвийн үйлдлүүдийг `action` талбараар нэг цэгт нэгтгэ. (2026-09-09-нд v533 ингэж унасан.)
- **Async ачаалалт → дахин зурах** гогцоог үргэлж НЭГ УДААГИЙНХ болго,
  эс бөгөөс мөнхийн давталтад орж сайт гацна.

## Шалгалтын систем Firestore-гүй (2026-09-09)
- Шалгалтын хуудас (`habea-exam.html`) Firebase SDK ОГТ ачаалдаггүй. Асуултыг `GET /api/exam-config/?exam=<key>` (зөв хариултгүй) авч, хариултаа `POST /api/exam-save/` руу илгээнэ — сервер R2 дахь `exams/config/<key>.json` (зөв хариулттай, хамгаалагдсан) дээр оноог тооцож `exams/_all.json` + `exams/<хэш>.json` + `exams/sig/<id>.json`-д бичнэ.
- ⚠ Дүнгийн эзэн = НОТЛОГДСОН и-мэйл: idToken, эсвэл otpId (зөвхөн үндсэн төслийн `where:'main'`), эсвэл аппын `vt/vexp/vem` эрх (олгосноос 6 цаг). Нотолгоогүй бол 403. Клиентийн бичсэн и-мэйлд итгэхгүй.
- `habea-admin.html` — аппын (үндсэн төсөл) админ нэвтрэлт; PIN алга. Тохиргоо `POST /api/exam-config {action:'get-full'|'save'}`, дүн R2 `exams/_all.json` (гарын үсэгтэй), устгах `POST /api/exam-save {action:'delete'}`.
- `api/exam-sync.js` — зөвхөн НЭГТГЭНЭ (хуучин Firestore мөрийг R2-д байхгүй бол нэмнэ); юу ч дарж бичихгүй, хоослохгүй. Cron-оос хасагдсан; гараар (админ) л дуудна.
- ⚠ Vercel функц ЯГ 12/12 — шинэ функц нэмэх бол аль нэгийг нэгтгэ (action-оор).
- Firestore дүрэм: `node tools/deploy-rules.js --key <sa.json>` (амьд дүрмийг `tools/rules-backup/`-д хадгалаад байршуулна; `--rollback <id>`). Консол дээр гараар БҮҮ тавь.
- Дүрмийн гол зарчим: ажилтан `users/{өөрийн}`-ийн role/department/position/email/uid/isActive-ийг өөрчилж ЧАДАХГҮЙ; `user_roles` зөвхөн админ; `kpi_*` устгах зөвхөн админ.

## «Excel (график)» — жинхэнэ Excel график/Pivot/Slicer (v565, 2026-09-13)

Excel-ийн график, PivotTable, Slicer-ийг **ямар ч JavaScript сан үүсгэж чаддаггүй**
(ExcelJS ч, SheetJS ч chart XML бичдэггүй). Тиймээс загварыг Excel дээр COM-оор
**нэг удаа** зохиож R2-д тавьсан, татах үед хөтөч зөвхөн датаг нь сольдог.

- Загвар: **`reports/templates/wk_dashboard.xlsx`** (43 КБ)
  - «Дата» хуудас — `WKDATA` нэртэй хүснэгт (ЗӨВХӨН энэ хэсэг солигдоно)
  - «Дашбоард» хуудас — 4 PivotTable + 4 PivotChart + 2 Slicer
  - `pivotCache` дээр `refreshOnLoad="1"` → Excel нээхдээ өөрөө дахин боднo
- Бүтээгч: `scratchpad/mk_template.ps1` · дүүргэгч (локал): `fill_template.ps1`
  · шалгагч: `check_filled.ps1` · амьд шалгалт: `t_xlstpl.js`
- Аппын тал: `rfXlsTpl()` — `data-rf-dl="tpl"` товч. **Одоогийн 12 хуудаст
  экспортыг ОРЛОХГҮЙ**, тусдаа гурав дахь товч.

### ⚠⚠ ТАВАН ДҮРЭМ — тус бүр нэг бүтээх мөчлөг зарцуулсан
1. **Загварыг ExcelJS-ээр ачаалж хадгалж БОЛОХГҮЙ** — график, pivot, slicer
   бүгд УСТАНА. Цорын ганц аюулгүй арга бол ZIP түвшинд зөвхөн хоёр хэсэг солих:
   `xl/worksheets/sheet1.xml` (шинэ мөрүүд) ба `xl/tables/table1.xml`
   (`ref="A1:H<N>"` — **хоёр газар**). Бусдыг хөндөхгүй.
2. **Толгойн 8 багана загварынхтай ЯГ ҮСЭГЧЛЭН таарна** (`RF_TPL_HEAD`).
   Нэг үсэг зөрвөл pivot-ын мөрийн талбар танигдахгүй, БҮХ ГРАФИК ХООСОН гарна.
3. **Pivot-ын тоолох талбар нь мөрийн талбартай ӨӨР багана байх ёстой.** Нэг
   талбарыг мөр ба дата хоёуланд нь тавьбал Excel мөрийн хэсгээс нь гаргаж,
   pivot зөвхөн нийт дүн харуулна (`TableRange1` = ганц багана, «Тоо» + «8»).
4. **Бүх pivot НЭГ дундын PivotCache-ээс** үүснэ — slicer зөвхөн ижил cache
   хуваалцсан pivot-уудыг удирддаг. Тус тусдаа cache үүсгэвэл
   `SlicerCaches.AddPivotTable` нь `0x800A03EC`-ээр унана.
5. Мөрүүдийг **inline string**-ээр бичнэ (`t="inlineStr"`) — тэгвэл
   `sharedStrings.xml`-д огт хүрэхгүй тул тэр хэсэг байт-ижил үлдэнэ.

### Дахин зохиомж (v566) — HTML дашбоардын БҮХ картыг хамруулав
Эхний хувилбар нь ажиллаж байгаагаа нотолсон техникийн загвар байсан бөгөөд
хэрэглэгч «огт таалагдахгүй байна» гэж буцаасан. Одоогийн байдал:
- **15 багана** (байршил, эрсдэлийн зэрэг, хугацааны биелэлт, шат, мэдээлэгч,
  гүйцэтгэгч, тайлбар нэмэгдсэн). Зарим нь `rfDashData`-гийн мөрийн жагсаалтаас
  **бодогдоно** (`d.onTime/late/waiting/unreliable`, `d.stage.*`).
- **12 PivotTable + 12 график** (нуусан «Тооцоо» хуудсан дээр pivot), **5 Slicer**,
  **5 KPI хайрцаг** (амьд `COUNTIF` томьёо).
- **Дарж задлах:** pivot-ын тоон дээр ДАВХАР ДАРВАЛ Excel шинэ хуудас үүсгээд тэр
  тооны ард байгаа бүх мөрийг гаргана (drill-through — төрөлхийн боломж).
  Slicer дээр дарвал 12 график зэрэг шүүгдэнэ.

### ⚠ Дахин зохиохдоо олдсон дөрвөн зүйл
1. **`fullCalcOnLoad="1"` заавал** (`xl/workbook.xml`). Pivot нь
   `refreshOnLoad`-оор шинэчлэгддэг ч **ТОМЬЁО** загварыг хадгалах үеийн утгаа
   барьдаг — 16 мөртэй датад KPI хайрцаг «8» гэж харуулж байв.
2. **Slicer-ийн өргөн 250+** байх ёстой, эс бөгөөс «Захиргаа, Хүний нөөцийн ал…»
   гэж таслагдана.
3. **Slicer-ийг графикаас хол багананд** тавь (R биш T) — эс бөгөөс баруун талын
   графикийн шошгыг халхална («2026-0…», «Тооцоонд оро…»).
4. **Цагийн цуваа нь нэрээр**, эрэмбэлсэн карт нь **тоогоор** эрэмбэлэгдэнэ.
   Бүгдийг тоогоор эрэмбэлбэл сар 03, 02, 05, 04 гэж эмх замбараагүй гарна.

### ⚠⚠ v567 — ГРАФИК ХУУЧИН ДАТА ХАРУУЛДАГ БАЙСАН (хамгийн далд алдаа)
Татсан файлыг нээхэд дээд талын том тоонууд **зөв** (33) гарч байсан ч **бүх
график 8** гэж харуулж байв — 8 нь загварын жишээ датаны мөрийн тоо.

**Шалтгаан:** `xl/pivotCache/pivotCacheRecords1.xml` нь загварыг хадгалах үеийн
мөрүүдийг хуулбарлан агуулсаар байдаг. **`refreshOnLoad="1"` ДАНГААРАА
хүрэлцэхгүй** — Excel тэр хуучин агшнаас графикийг зурчихдаг. (Томьёо нь
`fullCalcOnLoad`-оор шинэчлэгдэж байсан тул тоо ба график зөрж, алдаа улам
далд болсон.)

**Засвар (татах бүрд, PowerShell ба JS хоёуланд):**
- `xl/pivotCache/pivotCacheRecords1.xml` → `<pivotCacheRecords … count="0"/>`
- `xl/pivotCache/pivotCacheDefinition1.xml` → `recordCount="0"`, `refreshOnLoad="1"`
  **ба `invalid="1"`** ← v568-д нэмсэн, ГОЛ ТҮЛХҮҮР нь энэ

### ⚠⚠ v568 — `refreshOnLoad` ДАНГААРАА АЖИЛЛАХГҮЙ (4 оролдлогын дүн)
| Оролдлого | Үр дүн |
|---|---|
| `refreshOnLoad="1"` | файл нээгдэнэ, pivot **хуучин** утгаа барина (33 мөрт 8) |
| кэшийн бичлэгийг хоослох | мөн адил хуучин утга |
| бичлэгийг **бүрмөсөн устгах** | ⛔ Excel файлыг **нээхээс татгалзана** (багц зөв байсан ч) |
| **`invalid="1"`** | ✔ **АЖИЛЛАВ** — Excel нээхдээ эх сурвалжаас дахин бодно |

`invalid="1"` нь `pivotCacheDefinition` дээрх кэшийг «хүчингүй» гэж тэмдэглэдэг.

### Дахин зохиомж (v570, 2026-09-14) — «нэг дороос бүгдийг шүүх»
Хэрэглэгчийн гомдол: ① шүүлтүүр чартын гарчгийг ДАРЖ байсан, ② чартууд
уйтгартай, ③ гүйцэтгэгчээр шүүгээд «хэнээс ирсэн хэдэн ажлыг яаж гүйцэтгэсэн»-ийг
нэг дороос харах боломжгүй байсан.

**Шинэ бүтэц** (`scratchpad/mk_template.ps1` + `tpl_labels.json`):
- мөр 1-4: гарчиг, шар анхааруулга (Ctrl+Alt+F5), тайлбар
- мөр 6-9: **5 KPI карт** — өнгөт тууз + том тоо
- мөр 11, 13: **8 ШҮҮЛТҮҮР ХӨНДЛӨН** (4×2). Дээд эгнээ урт жагсаалт
  (Гүйцэтгэгч · Мэдээлсэн · Алба · Байршил) тул 268px, доод эгнээ богино
  (Төрөл · Төлөв · Сар · Хариуцах алба) тул 132px
- мөр 15+: **14 график** 2 баганатай сүлжээнд, төрөл нь холилдсон
  (бөгжин · шугам · хэвтээ багана · давхарласан · босоо багана)
- мөр 178+: **ДЭЛГЭРЭНГҮЙ ЖАГСААЛТ** — 12 талбартай tabular pivot. Гүйцэтгэгчийн
  нэр дарахад тухайн хүний БҮХ ажил мөр мөрөөрөө энд гарна. Excel дээр
  шүүлтүүрээр жолоодогдох хүснэгт хийх ЦОРЫН ГАНЦ зөв арга нь энэ.

**Гурван зарчмын шийдэл:**
1. **KPI нь COUNTIF БИШ, GETPIVOTDATA.** COUNTIF нь шүүлтүүрийг огт тоодоггүй —
   «Батболд» гэж шүүхэд 5 тоо нь бүгдийнх хэвээр үлдэнэ. ⚠ Тоон утгыг текст
   болон тоогоор ХОЁУЛАНГ нь хайж НЭМбэл Excel хөрвүүлээд давхар тоолно
   (28-аас 20 гэж гарсан) — үүрлэсэн `IFERROR` ашиглана.
2. **Өнгийг гараар биш, НОМЫН САНГИЙН ЗАГВАРААР.** Багана тус бүрийн өнгийг
   гараар тавибал зөвхөн загварын 8 мөрийн ангилалд хэрэгжинэ. `Theme
   .ThemeColorScheme.Colors(5..10)`-ыг сольбол шинэ ангилалд ч үйлчилнэ.
   ⚠ `VaryByCategories` БҮҮ асаа: accent 6 л байдаг тул 12 баганад Excel өөрөө
   хаки/хүрэн/бор сүүдэр зохиож муухай болгоно. Эрэмбийн график НЭГ өнгөтэй,
   өнгөний ялгаа нь чарт хооронд байна.
3. **Багана цөөрүүлж өргөсгө.** Дэлгэрэнгүй жагсаалт талбар тутамд нэг багана
   эзэлдэг тул 20×66px сүлжээнд бичвэр тасардаг байв. 15×113px (1700px) болгоход
   уншигдахуйц болсон. Хайрцгийн өргөнийг сүлжээнээс тооцоолно (5×3=15).

**Сүүдэр нэр (v570-д зассан):** загварын 8 жишээ мөрийн нэрс бүх шүүлтүүрт
саарал болж үлдэж, БАЙХГҮЙ хүнийг сонгуулж байв (локалаар 10 ширхэг).
`pivotCacheDefinition@missingItemsLimit="0"` нь засна. ⚠ COM-ийн
`xlMissingItemsNone` тогтмол нь **2** бөгөөд Excel түүнийг XML рүү шууд 2 гэж
бичдэг — тэнд «хоёрыг үлдээ» гэсэн утгатай тул хүчээр 0 болгох ёстой.
`rfXlsTpl` болон `fill_template.ps1` хоёуланд нь хийсэн.

**«Дугаар» багана (16 дахь).** `Огноо` нь `createdAt.slice(0,10)` — зөвхөн ӨДӨР.
Pivot нь харагдах бүх талбараараа ижил мөрүүдийг НИЙЛҮҮЛДЭГ тул нэг өдөр ижил
төстэй 2 ажил бүртгэгдвэл дэлгэрэнгүй жагсаалтад нэг мөр болж, бүртгэл чимээгүй
алга болох байв. `id` нэмснээр мөр бүр давтагдашгүй боллоо.

**Шалгалт — `scratchpad/test_dash.ps1` (8 бүлэг, бүгд ногоон):** объектын тоо ·
бүх дүрсийг тэгш өнцөгтөөр харьцуулж ДАВХЦАЛ хайх (энэ нь шүүлтүүрийн
өргөн/өндөр солигдсоныг барьсан) · чарт өөрийн эгнээндээ · нэг нэр дарахад
жагсаалт зөвхөн түүнийх (гараар тоолсонтой тулгана) · KPI шүүлтүүрийг дагах ·
Ctrl+олон нэр · хоёр дахь шүүлтүүр · сүүдэр нэр байхгүй.
⚠ Зураг хангалтгүй: анхны хувилбарт шүүлтүүр чарт дээр буусныг зөвхөн энэ
тооцоолол барьсан.

### v571 (2026-09-14) — «юуг засварласан» бичвэр + чартын хэмжээ
Хэрэглэгчийн хоёр гомдол, хоёулаа зөв байсан:

**① Чарт хэт том.** Мөрөнд 2 ширхэг, 840×320 байсан тул самбар 7 эгнээ болж
сунаж байв. `layout.chartPerRow: 3` болгоход 554×280, эгнээ 5 болов. Чартын
байрлалыг `$PERROW`-оос тооцдог тул цаашид JSON-ий нэг тоо солиход хангалттай.

**② «Юуг засварласан тухай мэдээлэл байхгүй».** Ажлын захиалгын тайланд
засварын тухай ЮУ Ч байгаагүй — «Тайлбар» гэсэн багана нь зөвхөн
`тийм/үгүй` **туг** байсан (тайлбар бичсэн эсэх), агуулга нь биш.

| багана | өмнө | одоо | эх сурвалж |
|---|---|---|---|
| 15 | «Тайлбар» тийм/үгүй | **«Асуудал»** жинхэнэ бичвэр | `r.desc` |
| 17 | *(байхгүй)* | **«Хийсэн ажил»** | `r.wkExecNote` |

`r.wkExecNote` бол аппын «Гүйцэтгэлийн тайлбар» — гүйцэтгэгчийн өөрийн
тэмдэглэл. ⚠ `r.wkHazFix`-ийг БҮҮ ашигла: тэр нь `{by, byName, at}` гэсэн
**объект**, текст биш. Хоёр багана хоёулаа доод талын дэлгэрэнгүй жагсаалтын
ТӨГСГӨЛД орно — урт бичвэр тул хажуугийн хоосон нүд рүү нэвчиж уншигдана.

**Бодит датаны байдал (28 бүртгэл):** «Асуудал» 28/28 бөглөгдсөн, «Хийсэн
ажил» 14/28. Талбар нь ашиглагддаг мөртлөө тайланд огт ордоггүй байжээ.
⚠ Туршилтын дата дээр тулгуурлан «төлөв бүрээр хэдэн хувь бөглөсөн» гэх мэт
дүгнэлт БҮҮ хий — `make_fill_rows.py` нь Төлөв/Шат/Яаралтай зэргийг id-ийн
хэшээр САНАМСАРГҮЙ үүсгэдэг (зөвхөн огноо, алба, байршил, мэдээлэгч,
гүйцэтгэгч, дугаар, асуудал, хийсэн ажил нь жинхэнэ).

### v571b (2026-09-14) — НЭГ ДЭЛГЭЦЭД багтаасан эцсийн байрлал
Хэмжээсийн нэгжийн алдаа зассаны дараа самбарыг хэрэглэгчийн дэлгэцэд
жинхэнээсээ тааруулав. **Хэрэглэгчийн сонголт:** эхний дэлгэцэд 6 график,
үлдсэн нь доор.

| | цэг | пиксел (125%) |
|---|---|---|
| Самбарын өргөн | 864 | 1440 (дэлгэц 1536) |
| Чарт (эхний 6) | 281×90 | 469×150 |
| Шүүлтүүр (6 ш) | 137 | 229 |
| Эхний дэлгэц дуусах | 389 | 648 (нүдний талбар ~665) |

Бүтэц: мөр 1-4 гарчиг/заавар · 6-9 KPI · 11 шүүлтүүрийн зурвас ·
13-26 **эхний дэлгэцийн 6 график** · 27 хуваах зураас · 29-64 үлдсэн 7 график
(илүү өндөр — нэрсийн эрэмбэ босоо зай шаарддаг) · 66+ дэлгэрэнгүй жагсаалт.

**Зургаас олдсон, тоогоор баригдаагүй гурван дутагдал:**
- «Албадаар» 9 албыг 150px өндөрт багтааж чадалгүй нэр/тоо нь давхцсан →
  эхний дэлгэцэд зөвхөн **≤5 ангилалтай** график тавина («Ажлын шат» солив).
- «Хэнээс хэнд ирсэн» (Мэдээлсэн×Гүйцэтгэгч) нь домог нь картын ихэнхийг
  эзэлж уншигдахгүй байсан → **хассан**. Тэр мэдээлэл дэлгэрэнгүй жагсаалтын
  «Мэдээлсэн» баганад бүрэн байгаа: гүйцэтгэгчээр шүүхэд хэнээс ирсэн нь мөр
  бүр дээр харагдана.
- ⚠ **`layout.slicerPerRow` нь `slicers` жагсаалтын уртаас ТУСДАА.** Жагсаалтыг
  7→6 болгоод энэ тоог 8 хэвээр орхиход шүүлтүүр 168px хэвээр үлдэж, баруун
  талд хоосон зай гарч, гарчиг нь тасарсаар байв. Хоёуланг нь ЗЭРЭГ өөрчил.

⚠ Эдгээрийг ямар ч автомат шалгалт барихгүй — **зургийг нь өөрөө хараад**
олсон. Тоон шалгалт (давхцал, багтац, мөрийн тоо) шаардлагатай ч хангалтгүй.

### v572 (2026-09-14) — нэг хүн, нэг нэр
**Шинж тэмдэг:** шүүлтүүрт «Д. Отгонбаатар» ба «Дагварэнцэн Отгонбаатар»
хоёр өөр хүн мэт гарч, нэрээр нь шүүхэд тухайн хүний ажлын ТАЛЫГ алддаг байв.

**⚠ Эхлээд ХЭМЖСЭН, дараа нь зассан.** «Нэр адилхан харагдаж байна» гэдэг нь
нэгтгэх үндэслэл БИШ — шийдэх шалгуур нь **uid**. `scratchpad/dupname.js`
(зөвхөн уншина) амьд дата дээр:

| Олдвор | Дүн |
|---|---|
| Нэг uid → хоёр бичиглэл | **4 хүн** |
| Нэг нэр → хоёр uid (давхар данс) | **0** |
| Хадгалсан нэр бүртгэлийнхээс зөрсөн | **22 uid** |

Давхар данс байгаагүй тул **нэгтгэх дата ч байгаагүй** — энэ нь харуулах
логикийн алдаа байлаа. Апп бүх дэлгэцэд `wkPersonName(uid, fallback)`-ийг
ашигладаг (19481, 20735, 20783-р мөр) атал **зөвхөн `rfTplRows` алгасаад**
хадгалагдсан мөрийг түүхийгээр нь авдаг байв. Хоёр дуудлагыг залруулав.

⚠ **R2 дахь `reports/_all.json`-д ГАР ХҮРЭЭГҮЙ.** Ямар ч бичлэг дарагдаагүй тул
алдагдах зүйл алга. Хэн нэгний нэр буруу бол **бүртгэлээс** нь засахад өмнөх
бүх тайлан дараагийн татахад зөв болно.

**Яагаад нэгддэг вэ:** `shortenPersonName` нь эхний үгийн эхний үсэг + «.» +
үлдсэн хэсэг болгодог тул «Б. Даваадорж» ба «Бямбасүрэн Даваадорж» хоёулаа
«Б.Даваадорж» болно — uid бүртгэлээс олдоогүй ч нийлнэ.

**Үр дүн (амьд v572 файл дээр):** гүйцэтгэгчийн 11 бичиглэл → **8**, хоёр
баганыг нэгтгэхэд **26 нэр = 26 хүн**, давхардал 0. Гурван нэр зэрэг сонгоход
19 → **22 мөр** (Отгонбаатарын хуваагдсан хоёр хэсэг нийлсэн).

**⚠ ШАЛГАГЧИЙГ ӨӨРИЙГ НЬ ЭХЛЭЭД ШАЛГА.** `scratchpad/verify_names.py`-г эхлээд
**засварын ӨМНӨХ** файл дээр ажиллуулж УНАХыг нь харсан. Унаж чаддаггүй
шалгалт давсан ч юу ч нотлохгүй. Мөн эхний хувилбар нь багана тус бүрээр л
шалгаж байсан тул «Б. Даваадорж» (гүйцэтгэгч) / «Бямбасүрэн Даваадорж»
(мэдээлэгч) гэсэн ЯГ ТЭР тохиолдлыг олж чадаагүй — хоёр баганыг нэгтгэж
шалгадаг болгосон.

### ⛔ `invalid="1"` — ХЭЗЭЭ Ч БҮҮ НЭМ (v568-д нэмж, v569-д буцаасан)
Кэшийг «хүчингүй» гэж тэмдэглэвэл Excel чимээгүй дахин бодохын оронд
хэрэглэгчид **«This PivotTable report is invalid. Try refreshing the data»**
гэсэн алдааны цонх гаргадаг. Хуучин тоо харуулж байсан нь ядаж алдаагүй
байсан тул энэ нь **дор**.

**⚠⚠ ЭНЭ ТӨСЛИЙН ХАМГИЙН АЮУЛТАЙ СОХОР ЦЭГ:**
1. Бүх COM шалгалт `DisplayAlerts = $false`-тэй ажилладаг (цонх автоматжуулалтыг
   гацаахгүйн тулд). Тэр тохиргоо **яг энэ төрлийн цонхыг ДАРДАГ** — зураг төгс
   харагдаж байхад хэрэглэгч алдаа авдаг.
2. Үүнийг барих гэж `scratchpad/check_pivot_valid.ps1` бичсэн: pivot-ын талбар,
   кэшийг COM-оор оролдож үзнэ. **АЖИЛЛАХГҮЙ** — `invalid="1"`-тэй файл дээр ч
   «12 valid, OK» гэж хэлсэн. Энэ скриптэд БҮҮ ИТГЭ.

**Дүгнэлт:** Excel файл нээхэд алдааны цонх гарах эсэхийг энэ орчинд
**автоматаар шалгах арга алга**. Pivot/кэшийн тохиргоонд гар хүрвэл
хэрэглэгчээр нээлгэж батлуулахаас өөр найдвартай зам байхгүй.

### ⚠⚠ ЭЦСИЙН БАЙДАЛ — Excel нээхэд pivot ӨӨРӨӨ дахин бодогддоггүй
`invalid="1"` нь **локал** файлд ажилласан ч **аппаас татсан** файлд ажиллаагүй.
Маш урт мөрдөн шалгалтын дараа **дараах бүгдийг нотолгоотойгоор НЯЦААВ** —
дахин бүү хөө:

| Таамаг | Дүн |
|---|---|
| Багцын бүтэц / JSZip-ийн сав | MD5-аар `sheet1`+`table1`-ээс бусад **бүх хэсэг байт-ижил** |
| Кэшийн төлөв | records хоосон, `recordCount="0"`, `refreshOnLoad`, `invalid` — хоёуланд ижил |
| Хүлээх хугацаа | 0 / 5 / 10 / 20 сек — бүгд ижил, өөрчлөгдөхгүй |
| Мөрийн тоо | локал дамжуулга **33 мөрөнд ч зөв** ажиллана |
| Хоосон утга, мөрийн бүтэц, халхлалт | цэвэр (34 мөр бүгд яг 15 нүд) |
| Хууль бус тэмдэгт, NBSP, ZWSP | алга |
| Багана доторх төрлийн холимог | профиль **яг ижил** (G тоон, бусад текст) |
| Толгойн бичвэр | **код тус бүрээрээ ижил** |

Солилтын туршилт: амьд багц + локал дата → **зөв**; локал багц + амьд дата →
**буруу**. Гэтэл хоёр датаны хуудсын хэмжигдэх бүх шинж ижил. Энэ зөрчил
тайлагдаагүй.

**ХЭРЭГЛЭГЧИЙН ШИЙДВЭР (2026-09-13):** ингээд үлдээнэ. Файлыг нээгээд
**«Дата → Бүгдийг шинэчлэх» (Ctrl+Alt+F5)** нэг удаа дарахад бүх график зөв
болно (бодитоор батлагдсан). Загварын дэд гарчигт энэ зааврыг **эхний
өгүүлбэр** болгон бичсэн. Drill-through ба Slicer бүрэн ажиллана.

⚠ Хэрэв хожим дахин оролдох бол: дээрх хүснэгтийн аль ч мөрийг бүү давт.
Үлдсэн бодит хувилбарууд нь (а) .xlsm + `Workbook_Open` макро (байгууллагын
орчинд макро хаагддаг), (б) pivot-гүй, зөвхөн томьёо/график (drill-through
алдагдана).
⚠ Бичлэгийн хэсгийг **устгаж болохгүй** — хоослоод үлдээнэ.
⚠ Шалгахдаа `RefreshAll()` бүү дууд (тэр нь алдааг нуудаг) бөгөөд ганц
удаагийн «зөв харагдлаа» гэдэгт бүү итгэ: **зургаар нь хараад** тоог нь нэг
бүрчлэн нэм. Энэ алдааг хоёр удаа «зассан» гэж андуурсан.

**⚠ ХЭМЖИЛТИЙН УРХИ — энэ нь намайг нэг удаа хуурсан:** шалгахдаа
`$wb.RefreshAll()` БҮҮ дууд. Тэр дуудлага яг энэ алдааг нуудаг бөгөөд «бүгд
зөв» гэсэн ХУДАЛ ногоон дүн өгсөн. Хэрэглэгч файлыг зүгээр нээдэг — шалгалт
тэр нөхцөлийг ЯГ давтах ёстой (`scratchpad/shot_sheet.ps1` одоо RefreshAll
дуудахгүй).

### PowerShell + COM урхи
- **`.ps1` файлыг цэвэр ASCII-гаар бич.** PowerShell 5.1 нь BOM-гүй файлыг ANSI
  гэж уншдаг тул кирилл мөр задлан шинжлэлтийг бүхэлд нь унагаана. Кирилл
  нэрсийг UTF-8 JSON-оос уншина (`tpl_labels.json`, `fill_rows.json`).
- **Excel COM нь процесс үлдээдэг.** Эхлэхийн ӨМНӨ `EXCEL.EXE`-ийн PID-уудыг
  тэмдэглээд, төгсгөлд зөвхөн ШИНЭ PID-ийг хаа — хэрэглэгчийн нээлттэй Excel-ийг
  хэзээ ч хаах ёсгүй.
- `ZipArchiveEntry.Crc32` гэсэн шинж **байхгүй** (.NET Framework). Түүгээр
  харьцуулбал бүх утга `$null` болж «ялгаа алга» гэсэн ХУДАЛ ногоон дүн гарна —
  жинхэнэ MD5 хэш ашигла.
- **⚠ PowerShell-ийн хувьсагчийн нэр ТОМ/ЖИЖИГ ҮСЭГ ЯЛГАДАГГҮЙ.** `$P` ба `$p`,
  `$XL` ба `$xl` бол НЭГ хувьсагч. 2026-09-13-нд хоёулаа хазсан: `$XL` (чартын
  төрлийн хүснэгт) Excel программын `$xl`-д дарагдаж «Does not support a
  collection» гэж бүтээлтийг унагаасан; `$P` (палитр) нь `foreach ($p in ...)`
  давталтад дарагдаж чартын өнгийг ЧИМЭЭГҮЙ алдагдуулах байсан. Богино нэр
  сонгохын өмнө жижиг үсгээр нь давхцаж байгаа эсэхийг шалга.
- **Функцээс 2 хэмжээст массив буцаахад `return ,(...)` гэж таслалтай бич.**
  PowerShell нь буцаах массивыг задалдаг тул `Range.Value2` нэг хэмжээст болж
  `GetValue($i,$j)` нь «Array was not a two-dimensional array» гэж унана.
- **`RPC_E_CALL_REJECTED` (0x80010001) бол алдаа биш — Excel завгүй байна.**
  Файлыг нээсний дараа pivot-ууд сэргэж дуустал COM дуудлагыг татгалздаг.
  0.5 сек тутам дахин оролддог бүрхүүлээр ороо (`test_dash.ps1`-ийн `Com`).
- **`Slicers.Add(...)` нь `Top, Left, ӨРГӨН, ӨНДӨР` дараалалтай** — өргөн нь
  түрүүлнэ. Сольж дамжуулсны улмаас шүүлтүүрүүд нарийн бөгөөд 320px өндөр болж,
  дээд эгнээ нь доод эгнээ рүүгээ, доод эгнээ нь чарт руу давхацсан (хэрэглэгчийн
  анхны гомдол яг энэ байсан).
- **`ColumnWidth` нь пиксел биш, ТЭМДЭГТЭЭР хэмжигддэг** бөгөөд номын сангийн
  стандарт фонтоос хамаарна. 8.43 гэж өгөхөд энд 1320px биш 996px гарсан —
  чарт шахагдаж, шүүлтүүрийн нэр тасарсан. Тавиад, ХЭМЖЭЭД, хүссэн пикселд
  хүртэл масштаблах давталт хэрэгтэй (`mk_template.ps1`).
- **Нэг хуудсан дээрх pivot бүрт ӨӨРИЙН БАГАНА хэрэгтэй.** Хөндлөн хүснэгт
  (багана талбартай) нь ажилтны жагсаалтын урттай тэнцүү өргөсдөг тул 300
  баганын зай үлдээнэ. Давхацвал «A PivotTable report cannot overlap another
  PivotTable report» гэж БҮХ бүтээлтийг унагаана.
- **⚠⚠ ХЭМЖЭЭС НЬ ПИКСЕЛ БИШ, ЦЭГ (point).** `Shape.Top/Left/Width/Height`,
  `Range.Left/Width`, `RowHeight` бүгд цэгээр. Хэрэглэгчийн дэлгэц 125%
  томруулалттай тул **1 цэг = 1.667 пиксел**. «1700» гэж тавьсан самбар
  үнэндээ **2836 пиксел** болж, 1536px дэлгэцээс бараг хоёр дахин өргөн байв —
  «чарт хэт том» гэсэн гомдлын ЖИНХЭНЭ шалтгаан энэ байсан. Дэлгэцийн
  ажлын талбарыг `[System.Windows.Forms.Screen]`-ээр уншаад, Excel-ийн тууз
  зэргийг хасч (≈250px) нүдний талбарыг гаргаад, 1.667-д хуваан ЦЭГ болгоно.
  Энэ хэрэглэгчид: 1536×912px → нүдний талбар ≈1536×665px → **921×399 цэг**.
- **⚠⚠ Хүснэгт хэлбэрийн pivot нь баганыг ХОЁР удаа өргөсгөдөг — хоёуланг нь
  зогсоо.** Урт бичвэртэй талбар («Асуудал», «Хийсэн ажил») байвал:
  ① **үүсэх мөчид НЭГ УДАА** автоматаар өргөсгөнө — «Огноо» 306 цэг, «Хийсэн
     ажил» 175 цэг болж, 864 цэгийн самбарыг 1230 цэг (2050px) болгосон;
  ② дараа нь **`RefreshAll` бүрд** дахин өргөсгөнө (1349 цэг хүртэл).
  `PivotTable.HasAutoFormat = $false` нь зөвхөн ②-ыг зогсооно. ①-ийг зогсоох
  арга алга тул: **pivot-ыг чарт/шүүлтүүр байрлуулахаас ӨМНӨ үүсгэ → баганын
  өргөнийг эргүүлж тааруул → геометрээ ДАХИН ХЭМЖ**, тэгээд байрлуул.
  ⚠ Оношлохдоо сэргээхээс ӨМНӨ ба ДАРАА хоёуланд нь хэмж. Зөвхөн «дараа»-г
  хэмжвэл «HasAutoFormat зассан» гэсэн худал дүгнэлтэд хүрнэ — үнэндээ
  өргөсгөлт нь угсралтын үед аль хэдийн болчихсон байсан.

### Нотолгоо (бодитоор хийгдсэн)
- MD5-аар: яг **2 хэсэг** өөрчлөгдсөн; график/pivot/slicer/sharedStrings
  **33 хэсэг байт-ижил**.
- Excel-д нээхэд pivot-ууд **шинэ** тоог харуулсан (8/4/2, албад 5/4/3/2,
  сараар 2/3/3/2/2/2, нийт 14) — `check_filled.ps1` 5/5.

## Ажлын захиалгын Excel — НҮҮР хуудас нь дашбоард (v562, 2026-09-13)

Татсан файлын **эхний хуудас** нь «Дашбоард» болов. Доорх 11 хуудас (Тойм, Бүх
мэдээлэл, Төрлөөр, Сараар, …) **нэр, дараалал, багана нь өөрчлөгдөөгүй**.

### ⚠ ЯАГААД SheetJS-ЭЭР ХИЙЖ БОЛОХГҮЙ ВЭ (буцааж бүү шилжүүл)
`xlsx@0.18.5`-ийн **нээлттэй хувилбар нүдний ЗАСВАРЫГ БИЧДЭГГҮЙ** — өнгө, тод
үсэг, нэгтгэсэн нүд бүгд худалдааны Pro хувилбарт л байдаг. Тиймээс түүгээр
«дашбоард» хийвэл өнгөгүй энгийн хүснэгтээс хэтрэхгүй. **ExcelJS 4.4.0** нь
аппад аль хэдийн ачаалагддаг (`woLoadExcelJS` — ажлын захиалгын маягт бөглөхөд)
бөгөөд өнгө/нэгтгэл/өргөнийг бүрэн дэмждэг.

**Excel-ийн ЖИНХЭНЭ график (chart) нь аль ч JS сангаар бичигдэхгүй** — ExcelJS ч
чаддаггүй. Тиймээс нүдэн доторх `█` баганан диаграм + өнгөт дүүргэлтээр
дүрсэлсэн. «Яагаад график алга вэ» гэж дахин хайх шаардлагагүй.

### Бүтэц
- `rfXlsDash(wb, d)` — нүүр хуудсыг зурна. Туслахууд: `rfXlsArgb` (hex→ARGB),
  `rfXlsBar` (20 блокт харьцуулсан диаграм), `rfXlsCell`, `rfXlsBlock` (хэсгийн
  гарчиг), `rfXlsTile` (гол тооны хайрцаг).
- Тоонууд нь **дэлгэц дээрхтэй ИЖИЛ** `rfDashData(all)`-аас гардаг тул Excel ба
  вэб хоёр зөрөх боломжгүй.
- `rfExportXlsx` одоо **async** (`await wb.xlsx.writeBuffer()`) — ачаалагчийн
  дотор `(async function(){…})()`-оор ороосон.

### Шалгалт
`scratchpad/t_xlsdash.js` — ЖИНХЭНЭ функцуудыг таслаж аваад **хуурамч ExcelJS**
дээр ажиллуулна. Хуурамч сан нь буруу нүдний хаяг, буруу нэгтгэсэн муж,
тодорхойлогдоогүй хувьсагчийг ШУУД барина; дутсан туслахыг өөрөө нөхдөг.
13 шалгуур, үүнд **хоосон өгөгдөл** (n=0) дээр ч унахгүй байх нь багтсан.
⚠ Excel-ийн экспортод гар хүрвэл энэ шалгалтыг ЭХЛЭЭД ажиллуул — хэрэглэгч
«татаж чадсангүй» гэж мэдэхээс өмнө алдаа барина.

Амьд шалгалт: `scratchpad/t_xlsdl.js` — жинхэнэ товчийг дарж, `URL.createObjectURL`-ыг
өөрчилж blob-ыг барьж, хуудсанд ачаалагдсан ExcelJS-ээр буцааж уншина
(`rfSaveBlob` нь IIFE дотор тул шууд барих боломжгүй). 12/12 тэнцсэн.

### ⚠⚠ ГУРВАН ХЭМЖИЛТИЙН УРХИ (тус бүр нэг ажиллалт үрсэн)
1. **Дашбоард бол ТУСДАА ТАБ** (`[data-rf-tab="dash"]`). `goTo('reportflow')` нь
   анхдагчаар «Ажлын захиалга» табыг нээдэг тул татах товч БАЙХГҮЙ. Эхлээд
   табыг дар.
2. **Өгөгдөл ачаалагдтал хүлээ.** `DB.reports` бүрдэхээс өмнө «Excel татах»
   дарвал БҮХ УТГА 0-тэй файл татагдана — диаграм ч зурагдахгүй («алдаа» гэж
   эндүүрч болзошгүй). Дэлгэц дээрх «Нийт мэдээлэл» тэгээс ялгаатай болтол хүлээ.
   ⚠⚠ ЭНЭ НЬ ЗӨВХӨН ТУРШИЛТЫН УРХИ БАЙГААГҮЙ — хэрэглэгч бодитоор өртөж,
     «Дашбоард нь ажиллахгүй байна» гэж мэдэгдсэн (2026-09-13). **v563-д
     зассан:** `rfWaitReports()` нь татахын өмнө өгөгдөл ирэхийг 15 хүртэл
     секунд хүлээж, ирэхгүй бол хоосон файл ТАТАХГҮЙ, шалтгааныг хэлнэ.
     Хамгаалалт нь `all.length === 0` дээр — `d.n === 0` дээр БИШ, учир нь
     хугацааны шүүлтээр 0 гарах нь ХУУЛЬ ЁСНЫ (тэр үед татагдах ёстой).
     Регрессийн шалгалт: `scratchpad/t_xlsearly.js` (өгөгдөл хүлээхгүй,
     хамгийн эрт дарж, 0-тэй файл гарахгүйг батална).
3. **`… | tail -N` нь жинхэнэ гаралтын кодыг ДАЛДАЛНА** (дамжуулалтын код нь
   `tail`-ийнх болно). Туршилтыг дамжуулалгүй ажиллуулж `$?`-ыг шалга — эс
   бөгөөс «унасан» шалгалт «амжилттай» мэт харагдана.

## ЭРХ: сургалт = НЭРЛЭСЭН ЖАГСААЛТ, эрсдэл = хөндөхгүй (v561, 2026-09-13)

**Эцсийн шийдвэр (хэрэглэгч).** Хоёр удаа эргэсэн тул эцсийн байдлыг эндээс унш:
1. «Туслах админ» гэсэн тусдаа эрх **БАЙХГҮЙ** — байсан ганцыг устгав
   (`user_roles` → `role=employee`; үлдсэн `depthead` = 0).
2. **Эрсдэлийн харагдац — v559-ийн хэвээрээ.** v560-д албаны удирдлагад албаныхаа
   бүх эрсдлийг үзүүлдэг болгосныг v561-д **БҮРЭН БУЦААВ** («яг өмнөх байсан хэвэнд нь»).
3. **Сургалтын харагдац — ЗӨВХӨН нэрлэсэн хүмүүст.** Өмнө нь `riskIsBoss()`
   (дарга|ахлах|менежер|эрхлэгч) шалгадаг тул **47 удирдлага** бүгд хардаг байв.
   Одоо `training/owners.json`-д нэрлэсэн **20 хүн** л харна.

### Хаана бичигдсэн бэ
- Нэрс: **`training/owners.json`** (R2) = `{updatedAt, updatedBy, map: {алба: [и-мэйл…]}}`.
  ⚠ Хүн нэмэх/хасах бол **КОД БИШ, ЭНЭ ФАЙЛЫГ** зас. Бэлэн хэрэгсэл: `scratchpad/owners_build.js`
  (нэгтгэж, хэн нэмэгдэж хэн хасагдахыг ил гаргана) → `scratchpad/put_owners.js` (бичнэ,
  өмнө нь нөөцөлж, завсрын өөрчлөлтийг шалгана).
- Код: `trnScope()` ④-р салаа ба `trndocScope()` — хоёул зөвхөн энэ жагсаалтыг уншина.
  `renderTrnDocs()` нь `trndocScope()`-оос **өмнө** `await trnOwnLoad()` хийнэ.

### Захирлын хамрах хүрээ — «Сургалтын баримт» (v564, 2026-09-13)
Өмнө нь `trndocScope()` нь `wkIsDirector()` бол болзолгүй `{all:true}` буцаадаг тул
**ямар ч захирал БҮХ албаны** сургалтын баримтыг хардаг байв. Гэтэл тэр хүн эрсдэл
болон сургалтын биелэлт дээр харьяа албаараа хязгаарлагддаг — гурван дэлгэц зөрж
байсан. Одоо `DIR_SCOPES`-ийн хүрээгээр шүүнэ (хэрэглэгчийн шийдвэр).
- **Гүйцэтгэх захирал** (`re = null`) — урьдын адил БҮГД.
- Бусад захирал — зөвхөн харьяа албад (ж: `/борлуулалт|маркетинг/i`).

⚠ Хамрах хүрээний объект нь өмнө «бүгд» эсвэл «**НЭГ** алба» гэсэн хоёрхон хэлбэртэй
байсан тул хоёр албыг илэрхийлж ЧАДДАГГҮЙ байв. Тиймээс `re` (хүрээний регекс) ба
`depts` (шошгонд харуулах нэрс) нэмэгдсэн. **Хуучин `dept` хэлбэр хэвээр ажиллана** —
нэрлэсэн хариуцагчид тэр замаар ирсээр байгаа тул түүнийг бүү устга.
Шалгалт: `scratchpad/t_trndoc.js` (харьяа 2 алба гарч, бусад алба ОРООГҮЙг батална).

### ⚠⚠ ГУРВАН ЗҮЙЛД ГАР ХҮРЭХГҮЙ
1. **`riskIsBoss()`-ыг сургалтын салаанд БУЦААЖ БҮҮ ТАВЬ** — 47 менежер бүгд дахин
   нээгдэнэ. Мөн өөрийг нь бүү зас: эрсдэл нэмэх эрх (`riskCanAdd`), ажлын захиалга
   (`taskIsBoss`), олон албанд эрсдэл нэмэх, нэг удаагийн ажлын самбар
   (`riskJobsPanelHTML`), ack ачаалалт гэсэн **5 өөр газарт** ашиглагдана.
2. **`risksForView()`-д хамрах хүрээ БҮҮ НЭМ** — тэр нь `ackReadOpen`/`ackOpenModal`-д ч
   дуудагддаг тул **ТАНИЛЦАХ/ГАРЫН ҮСГИЙН олонлогийг** тодорхойлдог. Тэнд гар хүрвэл
   ХАБЭА-н ажилтны гарын үсэг 19-ийн оронд 577 эрсдлийн төлөө бичигдэнэ. Харагдац
   өргөсгөх шаардлага гарвал ЗӨВХӨН `risksForPage()`.
3. **«Ажлын байрны үнэлгээ хийгдээгүй → албаныхаа БҮХ эрсдлийг харна»** гэсэн дүрмийг
   бүү эвд. Энэ нь `_riskSeenByRaw` дотор (`if (emp.dept)` салаа, scope `'dept'`/`'unit'`),
   **бүх ажилтанд** хамаарна, удирдлагын эрхтэй огт холбоогүй. Хэрэглэгч үүнийг тусгайлан
   сануулсан.

### Жагсаалтад захирал/ХАБЭА-г ҮЛДЭЭСЭН шалтгаан
`training/owners.json`-д байсан **Ц. Дэлгэрмаа (Санхүү хариуцсан захирал)**-гийн эрх нь
ЗӨВХӨН энэ файлаас гардаг — түүний албан тушаал `DIR_SCOPES`-ийн аль ч загварт
(гүйцэтгэх/үйлдвэрлэл/борлуулалт+маркетинг) тохирдоггүй. Хасвал эрх нь таслагдана.
**В. Энхбат (ХАБЭА-н ахлах менежер)** ч мөн адил үлдсэн. Тиймээс 18 + 2 = **20**.

### Хэмжилтийн урхи (энэ удаад хууртсан)
- `grep -c $'\r' file`-ийг **давхар хашилт доторх `$( )`-д бүү хий** — `$'\r'` задрахгүй тул
  бүх мөр таарч, «CRLF 37912» гэсэн ХУДАЛ дохио өгсөн. Зөв арга: python-оор
  `b.count(b'\r\n')`, эсвэл `git diff --numstat`.
- R2-гийн **шууд GET одоо `Forbidden`** өгдөг — уншихад ч `kind:'dl'` гарын үсэгтэй
  холбоос хэрэгтэй (`addowner.js` дэх хуучин загвар ажиллахаа больсон).
- Эрсдэлийн хуудасны тоо (`ЭРСДЭЛ БҮГД · N`) нь хадгалалтын мөрийн тоо **БИШ** —
  `riskEmpMerged()`-ээр нэгтгэсний дараах тоо (157→129, 221→107, 60→39, 79→67 гэж
  бодитоор баталсан). Тоо зөрөхөд «дата алдагдсан» гэж бүү дүгнэ.

## Найдвартай байдлын мөрдөн шалгалт — БОДИТ БҮРТГЭЛЭЭР (2026-09-13)

«Байн байн алдаа гарч, хэрэглэгчид татгалзаж байна» гэсэн гомдлыг таамаглалаар
биш, аппын өөрийн бүртгэлээр мөрдөв. ⚠ ЭХЛЭЭД ЭНЭ ХОЁР ФАЙЛЫГ УНШ:
- `sys/errors.json` — хэрэглэгчийн хөтөч дээр гарсан алдаа (төрөл бүр × тоо × хувилбар)
- `sys/clients.json` — `{updatedAt, rows: {<и-мэйл>: {v, at, n, dp, os, br, pwa, fb, noRec}}}`
  `fb` нь ТУХАЙН СЕССЭД Firestore-оос дүрэм зөрчиж уншсан цуглуулгууд (12 цагт нэг бичигдэнэ).

### Жинхэнэ олдсон, зассан зүйлс
1. **v556** — дашбоардын татаалт сонгосон хугацаанаас хэтэрдэг (`rfMonths` → `rfMonthsOf`).
2. **v557/v558** — эрсдэлийн хуудас аппыг БҮРЭН ХӨЛДӨӨДӨГ (`ACK_JOBS_LOADED` хэзээ ч
   тавигдахгүйгээс төгсгөлгүй `ackJobsLoadAll → renderHazards` давталт).
3. **v559** — `r2Grant()` ганц `fetch` хийдэг, дахин оролдлогогүй байсан тул сүлжээ
   хором зуур доголдоход ХАДГАЛАЛТ УНАДАГ. Одоо 3 оролдлого (400мс/1200мс), гэхдээ
   401/403 дээр шууд буцна. Шалгалт: `scratchpad/t_grant.js` (7/7).

### ⚠ ЯГААД НЬ БИШ — няцаасан таамгууд (дахин бүү хөө)
- **«Хэрэглэгчид хуучин код дээр гацсан»** — ҮГҮЙ. Сүүлийн 3 хоногт идэвхтэй 36
  төхөөрөмжийн БҮГД v548+ дээр. `sw.js` нь HTML/JS-ийг зориудаар кэшлэхгүй, `pwa-init.js`
  нь 2 минут тутам шалгаж, шаардвал SW-г устгаад дахин ачаалдаг.
- **«`seeded` тэмдэг алдагдсан»** — ҮГҮЙ. Амьд `reports/_all.json` дээр `seeded: true`
  байна; `repR2Merge` түүнийг зөв хадгалдаг (`seeded: !!(full || cur.seeded)`).
- **«Ачаалалтын бүрхүүл арилдаггүй»** — ҮГҮЙ. Шууд хэмжихэд **10 секундэд** арилсан.
  `_bootGuard` нь 10 секундын дараа `appReady()`-г try/catch-ийн ГАДНА болзолгүй дууддаг.
- **«JSON шахагдаагүй тул 2.3 MB дамждаг»** — ҮГҮЙ. Worker нь **Brotli** (`encoding: br`)
  ашигладаг; 2,331 KB гэдэг нь ЗАДАЛСНЫ ДАРААХ хэмжээ.
- **«`reqWorkDaysSince` давталт гацаадаг»** — ҮГҮЙ. `requests/_all.json` хоосон (0 мөр).

### Хэмжилтийн УРХИ (энэ нь надад ХУДАЛ алдаа заасан)
- `app-ready`-г MutationObserver-ээр хэмжвэл алдаж мэднэ — «120 секундэд болоогүй»
  гэсэн ХУДАЛ дүн гаргасан. ЗӨВ арга: `document.body.classList.contains("app-ready")`
  ба `#appBoot`-ийн `getComputedStyle`-ыг ШУУД унших (`scratchpad/t_ready.js`).
- Шүүрдэх шалгалт ажиллуулахдаа `sys/clients.json` руу бичихийг ХААХ — эс бөгөөс
  туршилтын хөтөч тухайн ажилтныг «шинэ хувилбар дээр» гэж ХУДАЛ тэмдэглэж,
  админы эрүүл мэндийн самбарыг бохирдуулна.

### Үлдсэн ажиглалт (хаагдаагүй)
`fb: kpi_reports` — 09-10…09-12-нд **9 хэрэглэгч** R2-оос `reports/_all.json` уншиж
чадалгүй Firestore руу ухарсан. Өнөөдөр давтагдсангүй (`t_boot.js` дээр зөвхөн
зориудын `kpi_hrorders` гарсан). Ачаалалт 21.5 сек (25-ын хязгаарт багтсан).
⚠ Дахин гарвал: `loadDB`-ийн 25 секундын хязгаар, `repR2Load`-ийн `seeded` шалгалт,
`r2Grant`-ийн эрх — гурвыг энэ дарааллаар шалга.

## Бүх эрхийн бүх цэсийн шалгалт ба олдсон 2 алдаа (2026-09-13)

Админ · албаны дарга · захирал · ХАБЭА · энгийн ажилтан — таван эрхээр цэс бүрийг
бодитоор дарж шалгав (`scratchpad/t_menus.js <эрх>`).

### ① Эрсдэлийн хуудас аппыг ХӨЛДӨӨДӨГ байсан (v558-д зассан)
Албаны даргын дансаар апп нээснээс хойш ~8–12 секундэд JS урсгал БҮРЭН зогсож,
аппаар огт ажиллах боломжгүй болдог байв. `Debugger.pause`-аар барьсан стек:
`renderHazards → riskAdminSectionsHTML → riskEmpStats/riskEmpMerged`.

**Шалтгаан:** `riskGroupedHTML` дотор
`ackJobsLoadAll().then(… renderHazards())` дуудагддаг атлаа `ackJobsLoad` нь
`if (jobs.length && allOk) ACK_JOBS_LOADED = true;` гэж ЗӨВХӨН нээгдсэн ажил
олдвол тугийг тавьдаг. Нээгдсэн ажилгүй хэрэглэгчид туг хэзээ ч тавигдахгүй тул
«ачаал → дахин зур → ачаал» гэсэн ТӨГСГӨЛГҮЙ давталт үүсдэг байв. Ажилтанд
эрхийн нөхцөл (`isAdmin() || isDeptHead() || riskIsBoss()`) биелдэггүй тул
тохиолддоггүй, админд тухайн үед ажил олдсон эсэхээс шалтгаалж ЗАРИМДАА л гардаг.

**Засвар:** `ACK_JOBS_TRIES` тоолуур (хамгийн ихдээ 3 оролдлого) + амжилттай
ачаалсан ч юу ч олдоогүй бол ДАХИН ЗУРАХГҮЙ. Мөн v557-д `riskAdminSectionsHTML`
доторх хэрэглэгддэггүй `riskEmpStats(list)` (262 × 892 = 233,704 дуудлага,
`if (false)` блокийн доторх үхсэн код) хасагдсан.

⚠ Ийм хэв маяг дахин үүсгэхгүй: **зурах функц дотроос ачаалалт эхлүүлээд,
дуусахад нь дахин зурах бол тугийг БҮХ тохиолдолд (дата хоосон ч) тавь**, эсвэл
оролдлогын тоолуур тавь.

### ② Дашбоардын татаалт сонгосон хугацаанаас хэтэрдэг байсан (v556-д зассан)
Огнооны муж сонгоод HTML/Excel татахад «бүх хугацаа» татагддаг гэсэн гомдол.
Мөрийн дата нь ЗӨВ шүүгддэг байсан («Огноо» багана мужид багтсан; «Дуусах
хугацаа» багана ирээдүйн огноотой байх нь зөв). Жинхэнэ согог нь сарын цуврал:
`rfMonths(n)` нь ӨНӨӨДРӨӨС хойш тогтмол n сар буцаадаг тул 7 хоног сонгоход ч
Excel «Сараар» 12 мөр, график 6 сар гаргадаг байв. `rfMonthsOf(d, cap)` нэмж,
сонгосон муж/хоногоор тооцдог болгов («Бүгд» үед датаны бодит хүрээгээр).
**Амьд баталгаа:** 09-07→09-13 сонгоход Excel «Сараар» 12 мөр → 1 мөр («9-р сар»),
HTML график 6 сар → 1 сар.

### ⚠ CDP цэсийн шалгалтын УРХИУД (эдгээрийг мэдэхгүй бол ХУДАЛ алдаа гарна)
- `daatgal` цэс нь аппаас ГАДАГШ шилждэг (`NAV_AWAY_PAGES`) — дарсны дараа
  буцаж ороод, апп ДАХИН тогтворжтол хүлээх ёстой. Эс бөгөөс дараагийн бүх цэс
  «хуудас алга» гэж харагдана.
- `adminpanel`, ажилтны `video-track` нь **iframe** (`/admin.html`,
  `/employee.html?embed=1`) — `innerText` 0 гарах нь ЗӨВ. iframe-ийн `src`-ээр үнэл.
- Апп ачаалж дуустал `#appBoot` бүх дэлгэцийг хаадаг (`body:not(.app-ready)`), тул
  жинхэнэ хэрэглэгч тэр үед цэс дарж чадахгүй. Програмчлан дарвал `restoreLastPage()`
  дарж бичээд «идэвхжээгүй» гэсэн ХУДАЛ дүгнэлт гарна.
- Хөлдөлт оношлохдоо `Debugger.enable`-ыг ГАЦАХААС ӨМНӨ асаа — гацсаны дараа
  enable хийвэл тэр өөрөө 90 секундэд хугацаа хэтэрнэ. Дараа нь `Debugger.pause`
  нь ажиллаж буй скриптийг таслаад стекийг өгнө. 250мс тутмын «цохилт» (console.log)
  нь гацсан агшныг миллисекундээр заана.

## ⚠ Тохиргооны файл дарагдсан осол ба ГУРВАН давхарга (2026-09-13)

**Юу болов:** ачаалалт 25 секундэд амжаагүй хөтөч дээр `DB` нь `seedDB()`-ийн хоосон
хуулбар болж үлдсэн. Тэр хөтчөөс ямар нэг зүйл хадгалахад `saveDB` → `kpiR2Publish`
183 KB тохиргоог **558 байт** болгож дарсан: 327 ажилтан, 32 модулийн нээлт, 132
хүний явц, туслах админы эрхүүд алга болсон. ⚠ Энэ зан төлөв v552-оос ӨМНӨ ч байсан —
өмнө нь Firestore нөөц байсан тул анзаарагддаггүй байв.

**Сэргээлт:** `state_restore.json` ← Firestore `kpi_state/main` (09-13 06:00:14Z, хамгийн
шинэ бүтэн хуулбар; R2 өдөр тутмын нөөц `backup/2026-09-12/kpi__state.json` нь 12 цаг
хуучин). Тамгатай (X-If-Match) PUT-аар буцаав: 187,152 байт, 327 ажилтан.

**ГУРВАН давхарга (дахиж давтагдахгүй):**
1. **v553** — `DB_LOAD_BROKEN`: ачаалалт таслагдвал тэмдэглээд `kpi/state.json` рүү
   ОГТ бичихгүй; дата хожуу бүрэн ирвэл хориг тайлагдана.
2. **v554** — `kpiR2Load` нь дутуу файлыг ХҮЧИНТЭЙ гэж үзэхээ болив (`settings` БОЛОН
   `moduleReleases`/`trainingModules` хоёулаа байх ёстой). Дутуу бол Firestore дэх
   бүтэн нөөц рүү шилжинэ → ажилтнуудад үйлчилгээ тэр дор нь сэргэнэ.
3. **v555** — `kpiR2Publish` нь `r2CasJson`-оор **серверийн хуулбарыг СУУРЬ болгож**
   зөвхөн өөрийн талбарыг дээрээс нь тавина. Серверт байгаад надад байхгүй талбар
   ХЭВЭЭР үлдэнэ — хэсэгчилсэн payload ч талбар устгаж чадахгүй.
   Шалгалт: `scratchpad/t_publish.js`.
4. Worker: `CAS_REQUIRED_KEYS`-д `kpi/state.json` нэмэгдсэн (тамгагүй бичилт → 428).

**Сургамж:** бүтэн объектыг дарж бичдэг («set») бичилт бол дата алдагдлын гол эх
үүсвэр. Хуваалцсан файлд ҮРГЭЛЖ нэгтгэл (merge) хийж, талбар/мөр алдагдахгүй эсэхийг
шалга. Мөн амьд туршилтад апп БҮРЭН ачаалахыг хүлээ — эс бөгөөс хагас ачаалсан хөгчөөр
жинхэнэ дата эвдэж болзошгүй.

## Firestore-оос салалт — KPI апп ЮУ Ч БИЧИХГҮЙ (2026-09-13)
Firestore-ийн өдрийн квот дүүрэхэд бичилт ЧИМЭЭГҮЙ уналт өгч, ажилтны бүртгэсэн
зүйл алга болдог байсан. Тиймээс **KPI апп Firestore руу бичихээ бүрэн болив**.
- **Бүртгэлүүд** (`violations, hazards, suggestions, incidents, videoViews, examResults,
  firstAidChecks, ppeObservations, extTrainings, externalTrainings`) → R2 `workflow/cols/<key>.json`.
  Бичилт: `colR2Sync()` — ЗӨВХӨН өөрчлөгдсөн цуглуулгыг, `riskR2PutJson` (CAS + ID нэгтгэл).
  Уншилт: `colR2Load()` — ачаалалтад эхэлж, ирсэн түлхүүрийг Firestore-оос ДАХИН асуухгүй.
  ⚠ Бичилт унавал сүүдэр (`_colShadow`) шинэчлэгдэхгүй → дараагийн хадгалалтад дахин илгээгдэнэ.
  ⚠ Шинэ цуглуулга нэмбэл `COL_R2_KEYS`-д бас нэм.
- `saveDB()` — админ ч, ажилтан ч Firestore руу бичихгүй: `colR2Sync` + (админ) `taskR2Sync`,
  `repR2Sync`, `kpiR2Publish(mainDocPayload())`.
- `saveCols()` — хоосон функц болсон (нийцлийн үлдэц). `r2Catalog()` — хоосон (Firestore `files`
  цуглуулгыг хэн ч уншдаггүй байсан). `reportPushToServer` — Firestore нөөц бичилтгүй.
- Firestore-оос УНШИХ нь сүүлчийн нөөц зам болж үлдэнэ (`kpi_state/main`, `kpi_tasks`, `kpi_reports`).
  `users`, `user_roles` — хэвээрээ (нэвтрэлт, ажилтны бүртгэл).
- Worker: `CAS_REQUIRED_KEYS`-д `workflow/cols/` угтвар нэмэгдсэн. ⚠ Байхгүй файл үүсэхдээ
  эхний тамгагүй PUT → 428 → 404 уншилт → `X-If-None-Match: *`. Энэ нь Worker-ийн 404/428
  хариунд ирдэг `X-Now`-оос «шинэ Worker мөн» гэдгийг сурдаг дээр тулгуурладаг.
- ⚠ АМЬД ТУРШИЛТЫН УРХИ: `saveDB()` нь `if (!fbReady) return;` дээр зогсдог тул апп
  БҮРЭН ачаалж дуусахаас өмнө UI-аар үйлдэл хийвэл сервер рүү ЮУ Ч бичигдэхгүй
  (2026-09-13-нд ачаалалт 30 секунд үргэлжилсэн). Бэлэн болсныг `sys/clients.json`
  бичигдсэнээр мэд. Эс бөгөөс туршилт «бичилт яваагүй» гэж БУРУУ дүгнэнэ.
- Амьд нотолгоо (2026-09-13, v552): PPE ажиглалт нэмэхэд
  `workflow/cols/ppeObservations.json 428` → `200 INM` (шинэ файл үүсэв),
  `tasks/all.json 200 IM`, `sys/clients.json 200 IM`; Firestore-ийн `kpi_state/main`
  бичигдсэн цаг ХӨДӨЛСӨНГҮЙ, `kpi_*` цуглуулгад нэг ч баримт нэмэгдээгүй.
- Шалгалт: `scratchpad/t_colr2.js` (18+1 шалгуур — зэрэг бичилт, унасан бичилт, устгал,
  шинэ файл үүсэх, хуучин Worker). ⚠ Хуурамч сервер бичихдээ ЖИНХЭНЭ Worker-ийн дүрмийг
  (404/428-д X-Now) дуурай — эс бөгөөс байхгүй файл үүсэх замыг буруу «унасан» гэж харуулна.

## Найдвартай бичилт — зэрэг бичилтийн хамгаалалт (2026-09-11)
Олон хүн нэг файлыг «уншаад → нэгтгээд → бичдэг» тул хамгаалалтгүй бичилт бусдын өөрчлөлтийг ЧИМЭЭГҮЙ дардаг (ажлын захиалгын батлалт ингэж алга болсон).
- **Апп:** олон хүн өөрчилдөг файлыг ЗААВАЛ `r2CasJson(key, build)`-аар бич. `build(cur)` цэвэр функц — оролдлого бүрт ШИНЭ хуулбар дээр дахин дуудагдана; `null` → бичихгүй; `{quiet:true}` → pulse дэгдээхгүй. Одоо ингэж бичигддэг: reports/_all.json, tasks/all.json, notify/_all.json, workflow/_deleted.json, workflow/_open.json, sys/clients.json, sys/errors.json, push/subs.json, requests/_all.json, workorders/_all.json, meetings/_weekly.json, ack/*.json.
- `riskR2PutJson` ч сүүлд уншсан тамгаар (X-If-Match) бичиж, 412/428 үед ID-гаар нэгтгэнэ: миний УНШСАН хуулбарт байсан атлаа бичилтэд байхгүй мөр = устгал; уншаагүй мөр = үлдэнэ. ID давхардсан жагсаалтыг нэгтгэхгүй.
- **Ажлын захиалга:** `wkPatch` эсвэл `reportPushToServer(r, {patch})` — зөвхөн өөрчилсөн талбар, серверийн шинэ мөр дээр 3 талт нэгтгэл (танигдах элементтэй жагсаалтыг элемент бүрээр). `{notify:true}` зөвхөн шинээр үүсгэхэд. Серверт хүрээгүй бичилт `localStorage.kpi_rep_outbox_v1`-д хадгалагдаж дахин илгээгдэнэ.
- **Даалгавар:** `TASK_BASE` (ачаалсан хуулбар) — бичихдээ «суурь → одоо» ялгааг серверийн шинэ мөр дээр тавина.
- **Цаг:** тамгыг `nowIso()` (Worker-ийн X-Now-оос сурсан серверийн цаг)-аар бич; төхөөрөмжийн буруу цаг бусдыг дарахгүй.
- **Сервер (api/):** `require('./_r2cas.js').casJson(key, build)` — аудитын гинж, шалгалтын дүн/синк, сэрэмжлүүлэг, push бүртгэл.
- **Firestore:** хүний нөөцийн тушаал `hrPushToServer` — гүйлгээ (runTransaction) + нөхөөс (`HR_BASE`); админы `saveCols` зөвхөн өөрчилсөн талбарыг `mergeFields`-ээр бичнэ, hrorders-ыг огт бичихгүй.
- **Worker** (`cloudflare/`): GET → `X-Etag`, `X-Now`; PUT → `X-If-Match` (зөрвөл 412), `X-If-None-Match: *` (байхгүй үед л үүсгэ), `CAS_REQUIRED_KEYS` дахь файлд тамгагүй PUT → 428. Deploy: `cd D:\monos-hab-lms\cloudflare && npx wrangler deploy`.
- ⚠ **ХАТУУ хаалттай (428)** — 2026-09-11-ээс Worker `CAS_REQUIRED_KEYS`: reports/_all.json, tasks/all.json, notify/_all.json, workflow/_deleted.json, workflow/_open.json, sys/clients.json, sys/errors.json, push/subs.json, requests/_all.json, workorders/_all.json, meetings/_weekly.json, ack/, audit/, exams/_all.json, exams/_index.json. Эдгээрт скрипт, туршилт, серверээс бичихдээ ЗААВАЛ `X-If-Match` (эсвэл шинэ файлд `X-If-None-Match: *`) хэрэглэ. `cloudflare/wrangler.toml` нь .gitignore-д байдаг тул жагсаалтыг энд давхар тэмдэглэв.
- ⚠ Шинэ хуваалцсан файл нэмбэл r2CasJson ашиглаад, шаардлагатай бол `CAS_REQUIRED_KEYS`-д нэм.
- Шалгалт (scratchpad): `t_cas.js`, `t_reliab.js`, `t_reliab2.js`, `t_srvcas.js`, `t_srvcas2.js`; амьд: `t_worker_cas2.js`. ⚠ Урт туршилтыг node-ийн нэрийг өөрчилсөн хуулбараар ажиллуул (n8n ажиглагч node.exe-г хаадаг).
